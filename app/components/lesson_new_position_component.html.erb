<%= form.govuk_collection_select(
      :new_position,
      collection,
      :position,
      :title,
      options: { selected: selected },
      label: { text: "Which lesson should this lesson appear after?" },
      ) %>
